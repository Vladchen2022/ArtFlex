# STATUS

最后更新：2026-03-20

本文件记录 **当前仓库代码可确认的真实状态**。  
它用于回答两个问题：

- 现在这个项目已经有什么
- 哪些部分已经定下，哪些部分仍在继续打磨

## 总体状态

当前仓库已经是一个可运行的 macOS Metal 绘图应用原型，不再是单纯壳层。  
主工作流已经形成，且最近一轮重点完成了：

- 选区工具主链
- 自由变形主链
- 自定义笔尖设计
- 画笔库持久化 / 导入导出
- 录像工具第一版
- 画布旋转工具
- 顶栏 / 左栏 / 右侧检查器的较大规模 UI 收口

当前最明显仍在继续打磨的方向是：

- 画布缩放流畅性
- 自由变形缩放/旋转后的最终像素质量
- 若干局部 UI 细节

## 当前代码里已经确认存在的主界面结构

### 主窗口

当前主界面包含：

- 顶部工具栏
- 左侧竖向工具栏
- 中央视图画布
- 右侧两列检查器

### 右侧检查器

当前右侧按代码可见的主要面板是：

- 左列：
  - 创意图形生成器（目前为空占位面板）
  - 颜色
  - 画笔库
- 右列：
  - 笔尖形状设计
  - 画笔参数
  - 图层

### 左侧底部附加入口

除了主工具按钮外，左侧底部现在还有：

- 录像工具按钮
  - 带录制状态点
  - 点击弹出录像设置面板

## 当前代码里已经确认存在的核心工具

当前 `ToolKind` 和主交互链中可确认存在这些工具：

- 画笔
- 橡皮擦
- 吸管
- 油漆桶
- 套索选区
- 多边形选区
- 套索填充
- 矩形选区
- 椭圆选区
- 直线
- 直线渐变
- 扇形渐变
- 涂抹
- 画布旋转
- 自由变形

说明：

- 这些工具当前都已经接入主工具栏 / 快捷键 / 主窗口交互链
- 但自由变形的缩放/旋转体验仍属于“已可用、可继续打磨”的状态

## 当前代码里已经确认存在的编辑能力

### 文档 / 图层 / 历史

当前可确认：

- 文档默认包含 1 个图层
- 默认图层名是 `图层 1`
- 图层支持：
  - 新增
  - 删除
  - 复制
  - 重命名
  - 拖拽排序
  - 显示 / 隐藏
  - 锁定 / 解锁
  - 透明度调整
  - 向下合并
  - 合并可见
- 历史系统已建立
- 当前撤销步数仍不设上限

### 选区链

当前代码已形成一条比较完整的选区主链：

- 套索选区
- 多边形选区
- 矩形选区
- 椭圆选区
- 选区增选 / 减选
- 选区移动
- 选区删除 / 填充

已知状态：

- 套索首个选区的光滑性问题已经收住
- 增减选功能本体是对的
- 蚂蚁线显示链已经基本稳定，但布尔轮廓视觉还不是最终最完美版本

### 自由变形

当前自由变形工具已具备：

- 有选区时移动选区内容
- 无选区时移动当前图层可见内容
- 变形框与 handle
- 平移
- 非等比缩放
- 旋转
- `Enter` 应用
- `Esc` 取消

当前状态：

- 移动主链已经可以正常使用
- 变形（缩放 / 旋转）主链已经打通
- 仍属于“功能已具备，后续可继续打磨质量和性能”的部分

### 画布视图工具

当前画布视图相关能力包括：

- 缩放
- 平移
- 重置视图
- 画布旋转

其中：

- `cmd +` / `cmd -` 已改为固定缩放档位
- `[` `]` 已改为非线性笔刷大小步进
- 画布旋转工具 `R` 已接通，并有 HUD 与回正按钮

## 当前代码里已经确认存在的笔刷系统

这是当前仓库里最完整的一条产品链之一。

### 笔尖形状设计

当前代码可确认：

- 存在独立的小画布
- 可手动画笔尖截面
- 支持：
  - 擦除
  - 从图片导入
  - 剪贴板粘贴导入
  - 拖拽图片导入
  - 插入形状
  - 喷墨散点
  - 旋转
  - 左右翻转
  - 上下翻转
- 导入图片会转为灰阶笔尖 mask
- 手动画预览与图片导入预览逻辑已分离，避免互相带坏

### 画笔参数

当前 `BrushSettings` 中已经存在并被 UI 使用的主要参数包括：

- `size`
- `opacity`
- `buildMode`
- `tipShape`
- `spacingPercent`
- `scatterAmount`
- `jitterAmount`
- `colorJitterAmount`
- `stampRotationDegrees`
- `followsStrokeDirection`
- `pressureSensitivity`
- `sizeLowerBound`
- `pressureSizeAmount`
- `pressureOpacityAmount`
- `customTipMaskData`
- `customTipSoftness`
- `customTipRoundness`
- `customTipAngleDegrees`

另外已存在：

- 尺寸压感曲线
- 透明度压感曲线
- 不透明度封顶模式
- 跟随笔迹方向图标切换

### 杂色功能

当前杂色功能已经调整为：

- 杂色色带方向始终跟随笔触行进方向
- 不再受笔刷自身旋转影响

### 画笔库

当前代码可确认：

- 画笔库使用格子槽位而不是简单列表
- 前 4 个槽位显示 `1 / 2 / 3 / 4`
- 在任何工具下按 `1 2 3 4`，如果对应槽位有笔刷：
  - 会切回画笔工具
  - 并激活对应笔刷
- 默认内置的 3 个旧笔刷已移除
- 画笔库现在会持久保存到应用级目录
- 重新启动 / 重新编译后自定义笔刷仍存在
- 支持右键：
  - 导出画笔库
  - 导入并替换
  - 导入并追加

## 当前代码里已经确认存在的颜色系统

### 颜色面板

当前颜色系统是独立状态对象，而不是简单单色板。

当前可确认：

- 有两种模式：
  - `picker`
  - `blocks`
- 颜色面板顶部操作按钮已改成图标按钮
- 色块模式已修正：
  - 吸管取色不会自动破坏当前色块组合
  - 只有显式同步时才把当前颜色同步进色块组合
- 色块模式支持三种从图片生成色块组合的方式：
  - 文件导入
  - `cmd+V`
  - 拖拽图片进入色块区域

## 当前代码里已经确认存在的录像工具

录像工具是最近一轮新增的重要能力。

当前可确认：

- 入口位于左侧工具栏底部按钮
- 录制状态点：
  - 红：录制中
  - 绿：未录制
- 录制设置弹出面板支持：
  - 录像目录
  - 捕获间隔
  - JPEG / PNG
  - 画质
  - 分辨率缩放
  - 自动开始录制
  - 开始 / 停止录制
  - 导出视频
  - FPS 预设
  - 开头停留 / 结尾停留
  - 打开当前录像目录
  - 打开导出视频
- 录制策略：
  - 只在文档内容变化时录帧
  - 且受最小捕获间隔限制
  - 写图走后台队列
  - 有队列上限和已跳过帧计数
- 同一文档的录制帧会固定落到同一个文档目录
- 已保存文档使用：
  - 文档名 + 路径指纹
  避免同名不同文件冲突

## 当前代码里已经确认存在的文件能力

当前可确认：

- 打开工程
- 保存工程
- 导出 PNG
- 新建画布弹窗

新建画布弹窗已支持：

- 预设比例
- 自定义宽高
- 横版 / 竖版切换
- 分辨率输入
- 比例联动
- 当前有未保存内容时的保存 / 放弃 / 取消确认

## 当前最近一次正在推进的技术主线

当前最近一次明确正在推进，但还没有宣布定版的是：

- **画布缩放流畅性改进**

当前已落到代码的第一步是：

- `CanvasPresentation` 已把“基础 fit 尺寸”和“zoomScale”拆开
- `CanvasContainerView` 中央文档内容组已从“缩放改 frame”改成“缩放改 transform”

当前状态判断：

- 这是已经开始的重构，不是最终完成版
- 下一步如果继续开发，最自然的主线仍然是继续压缩缩放时的显示/重绘成本

## 当前建议

如果现在继续恢复开发，建议优先阅读：

1. [STATUS.md](/Users/victorcloux/Desktop/ArtFlex/STATUS.md)
2. [CURRENT_TASK.md](/Users/victorcloux/Desktop/ArtFlex/CURRENT_TASK.md)
3. [DECISIONS.md](/Users/victorcloux/Desktop/ArtFlex/DECISIONS.md)
4. [XCODE_NOTES.md](/Users/victorcloux/Desktop/ArtFlex/XCODE_NOTES.md)

不要再把旧的 [CURRENT_STATUS.md](/Users/victorcloux/Desktop/ArtFlex/CURRENT_STATUS.md) 当作最新状态来源。
